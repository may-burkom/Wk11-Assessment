<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style.css">
    <title>Wk 11 Assessment</title>
</head>
<body>
    <div id="container">
        <h1>Welcome to Saint Joseph's Hospital.</h1>
        <hr>
        <div>
            <h4>User found:</h4>
            <p> <%= foundUser.name %></p>
            <p><button id="<%= foundUser._id %>">DELETE</button></p>
        </div>

        <a href="/">Return Home</a>

    </div>

    <script>
        const deleteBtn = document.querySelector("button");

        deleteBtn.addEventListener("click", function(event){
            event.preventDefault()
            let btnId = deleteBtn.id

            let xhr = new XMLHttpRequest()
            let deleteRoute = `http://localhost:8080/delete-staff/${btnId}`
            xhr.open("DELETE", deleteRoute, true)
            xhr.send()

            alert("Staff member deleted")
});
    </script>
    
</body>
</html>